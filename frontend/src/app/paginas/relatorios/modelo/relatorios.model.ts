import { RecursoPadraoModelo } from 'src/app/compartilhados/modelos/recurso-padrao.model';

export interface RelatoriosModelo extends RecursoPadraoModelo {
  // nome?: string;
  // instalacaoCodigo?: number;
  // siglaRegiao?: string;
  // siglaEstado?: string;
  // municipio?: string;
}
